# Resources.cs is generated by the GenerateCosturaResourcesFile MSBuild target, which always writes LF. Without
# this, core.autocrlf checks the file out with CRLF and every build leaves the working tree dirty.
src/Metalama.Community.Costura/Metalama.Community.Costura.Weaver/Resources.cs eol=lf
